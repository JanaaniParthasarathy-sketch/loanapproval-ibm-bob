version: "3.8"

services:
  loan-advisor-ai:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./models:/app/models
      - ./data:/app/data
